version: 0.2

phases:
  install:
    commands:
      - npm install
  pre_build:
    commands:
  build:
    commands:
      - echo we be buildn
      - npm run build
  post_build:
    commands:
      - aws s3 sync ./dist $S3_BUCKET
      - echo nothing to do
      - curl https://10zbellahd.execute-api.eu-west-2.amazonaws.com/default/discordGitInfo?msg=Buildsucess
artifacts:
  files:
    - '**/*'
  base-directory: dist